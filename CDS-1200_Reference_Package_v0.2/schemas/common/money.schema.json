{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:cds:schema:common:money",
  "version": "0.2.0",
  "title": "CDS Money",
  "$comment": "Amount is a string decimal in major units (e.g. \"129.95\") to avoid binary floating point; integer minor units were considered and rejected because string decimals match the measurement schema convention already in the package (CDS1100-10).",
  "type": "object",
  "properties": {
    "amount": {
      "type": "string",
      "pattern": "^-?\\d+(\\.\\d+)?$"
    },
    "currency": {
      "type": "string",
      "pattern": "^[A-Z]{3}$",
      "$comment": "ISO 4217 alphabetic currency code."
    }
  },
  "required": [
    "amount",
    "currency"
  ],
  "additionalProperties": false
}
